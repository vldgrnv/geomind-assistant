const API = '';
const token = localStorage.getItem('token');
if (!token) window.location.href = '/static/login.html'; const headers = { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token,
}; let currentChatId = null;
let lastAssistantText = '';
let lastUserQuery = '';
const SCRIPT_PROMISES = new Map(); const ICON_COPY = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>`;
const ICON_CHECK = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>`;
const ICON_REGEN = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 4v6h6"/><path d="M3.51 15a9 9 0 102.13-9.36L1 10"/></svg>`; if (typeof marked !== 'undefined' && marked.use) { marked.use({ gfm: true, breaks: true });
} /** Разрывает «стену текста», когда модель не поставила переносы перед заголовками и шагами. */
function normalizeAssistantMarkdown(raw) { if (!raw || typeof raw !== 'string') return ''; let t = raw.replace(/\r\n/g, '\n').trim(); t = t.replace(/([^\n#])(#{1,6}\s)/g, '$1\n\n$2'); t = t.replace(/([.!?;»])\s*(\d{1,2}\.\s)/g, '$1\n\n$2'); t = t.replace(/([а-яёА-ЯЁa-zA-Z)\]”—])\s*(\d{1,2}\.\s+)/g, '$1\n\n$2'); return t;
} function escapeHtml(s) { const d = document.createElement('div'); d.textContent = s; return d.innerHTML;
} function renderAssistantHtml(text) { const normalized = normalizeAssistantMarkdown(text); let html; if (typeof marked !== 'undefined' && marked.parse) { html = marked.parse(normalized); } else { html = '<p>' + escapeHtml(normalized).replace(/\n{2,}/g, '</p><p>').replace(/\n/g, '<br>') + '</p>'; } if (typeof DOMPurify !== 'undefined' && DOMPurify.sanitize) { return DOMPurify.sanitize(html, { ALLOWED_TAGS: ['p', 'br', 'strong', 'em', 'b', 'i', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'ul', 'ol', 'li', 'code', 'pre', 'blockquote', 'a', 'hr', 'table', 'thead', 'tbody', 'tr', 'th', 'td'], ALLOWED_ATTR: ['href', 'title', 'colspan', 'rowspan'], }); } return escapeHtml(normalized).replace(/\n/g, '<br>');
} const chatList = document.getElementById('chat-list');
const messagesDiv = document.getElementById('messages');
const emptyState = document.getElementById('empty-state');
const queryInput = document.getElementById('query-input');
const sendBtn = document.getElementById('send-btn');
const newChatBtn = document.getElementById('new-chat-btn');
const logoutBtn = document.getElementById('logout-btn');
const exportDocxBtn = document.getElementById('export-docx-btn');
const exportPdfBtn = document.getElementById('export-pdf-btn');
const reportBugBtn = document.getElementById('report-bug-btn');
const reportBugPanel = document.getElementById('report-bug-panel');
const reportBugText = document.getElementById('report-bug-text');
const reportBugSendBtn = document.getElementById('report-bug-send-btn');
const reportBugStatus = document.getElementById('report-bug-status'); function injectAdminLink(isAdmin) { if (!isAdmin) return; const sidebarBottom = document.querySelector('.sidebar-bottom'); if (!sidebarBottom || document.getElementById('admin-link-btn')) return; const link = document.createElement('a'); link.href = '/admin'; link.className = 'btn btn-outline btn-full'; link.id = 'admin-link-btn'; link.textContent = 'Админ-панель'; sidebarBottom.prepend(link);
} function updateExportButtons() { const disabled = !lastAssistantText.trim(); if (exportDocxBtn) exportDocxBtn.disabled = disabled; if (exportPdfBtn) exportPdfBtn.disabled = disabled;
} function setLastAssistantText(text) { lastAssistantText = typeof text === 'string' ? text : ''; updateExportButtons();
} function setBugReportStatus(text, type = '') { if (!reportBugStatus) return; reportBugStatus.textContent = text; reportBugStatus.className = 'report-bug-status' + (type ? ' ' + type : '');
} function getLastAssistantMessage(msgs) { for (let i = msgs.length - 1; i >= 0; i -= 1) { if (msgs[i].role === 'assistant' && typeof msgs[i].text === 'string') { return msgs[i].text; } } return '';
} function buildExportFileName(ext) { const now = new Date(); const pad = (v) => String(v).padStart(2, '0'); const stamp = [ now.getFullYear(), pad(now.getMonth() + 1), pad(now.getDate()), ].join('-') + '_' + [pad(now.getHours()), pad(now.getMinutes())].join('-'); return `geomind-last-answer-${stamp}.${ext}`;
} function loadExternalScript(src) { if (SCRIPT_PROMISES.has(src)) { return SCRIPT_PROMISES.get(src); } const promise = new Promise((resolve, reject) => { const existing = document.querySelector(`script[src="${src}"]`); if (existing) { if (existing.dataset.loaded === 'true') { resolve(); return; } existing.addEventListener('load', () => resolve(), { once: true }); existing.addEventListener('error', () => reject(new Error('script load failed')), { once: true }); return; } const script = document.createElement('script'); script.src = src; script.crossOrigin = 'anonymous'; script.async = true; script.addEventListener('load', () => { script.dataset.loaded = 'true'; resolve(); }, { once: true }); script.addEventListener('error', () => reject(new Error(`failed to load ${src}`)), { once: true }); document.head.appendChild(script); }); SCRIPT_PROMISES.set(src, promise); return promise;
} async function ensureDocxExportLibs() { await loadExternalScript('https://cdn.jsdelivr.net/npm/docx@8.5.0/build/index.umd.min.js'); await loadExternalScript('https://cdn.jsdelivr.net/npm/file-saver@2.0.5/dist/FileSaver.min.js');
} async function ensurePdfExportLibs() { await loadExternalScript('https://cdn.jsdelivr.net/npm/pdfmake@0.2.10/build/pdfmake.min.js'); await loadExternalScript('https://cdn.jsdelivr.net/npm/pdfmake@0.2.10/build/vfs_fonts.min.js');
} function markdownToPlainLines(text) { return normalizeAssistantMarkdown(text) .replace(/\r\n/g, '\n') .split('\n') .map((line) => line.replace(/\s+$/g, ''));
} async function exportLastAnswerDocx() { if (!lastAssistantText.trim()) return; try { await ensureDocxExportLibs(); } catch (e) { addMessageBubble('assistant', 'Не удалось загрузить библиотеки для экспорта DOCX.'); return; } const paragraphs = markdownToPlainLines(lastAssistantText).map((line) => { const trimmed = line.trim(); if (!trimmed) { return new docx.Paragraph({ text: '' }); } const headingMatch = trimmed.match(/^(#{1,6})\s+(.*)$/); if (headingMatch) { const levelMap = { 1: docx.HeadingLevel.HEADING_1, 2: docx.HeadingLevel.HEADING_2, 3: docx.HeadingLevel.HEADING_3, 4: docx.HeadingLevel.HEADING_4, 5: docx.HeadingLevel.HEADING_5, 6: docx.HeadingLevel.HEADING_6, }; return new docx.Paragraph({ text: headingMatch[2], heading: levelMap[headingMatch[1].length], spacing: { after: 120 }, }); } return new docx.Paragraph({ text: trimmed.replace(/^[-*]\s+/, '• ').replace(/^\d+\.\s+/, (m) => m), spacing: { after: 120 }, }); }); const documentFile = new docx.Document({ sections: [{ properties: {}, children: paragraphs, }], }); const blob = await docx.Packer.toBlob(documentFile); saveAs(blob, buildExportFileName('docx'));
} function exportLastAnswerPdf() { if (!lastAssistantText.trim()) return; ensurePdfExportLibs() .then(() => { const content = markdownToPlainLines(lastAssistantText).map((line) => { const trimmed = line.trim(); if (!trimmed) { return { text: ' ', margin: [0, 0, 0, 8] }; } const headingMatch = trimmed.match(/^(#{1,6})\s+(.*)$/); if (headingMatch) { const fontSizeMap = { 1: 18, 2: 16, 3: 15, 4: 14, 5: 13, 6: 12 }; return { text: headingMatch[2], bold: true, fontSize: fontSizeMap[headingMatch[1].length], margin: [0, 8, 0, 6], }; } return { text: trimmed.replace(/^[-*]\s+/, '• '), fontSize: 12, margin: [0, 0, 0, 6], }; }); pdfMake.createPdf({ pageSize: 'A4', pageMargins: [40, 48, 40, 48], defaultStyle: { font: 'Roboto', fontSize: 12, }, content, }).download(buildExportFileName('pdf')); }) .catch(() => { addMessageBubble('assistant', 'Не удалось загрузить библиотеки для экспорта PDF.'); });
} function applyStats(stats) { document.getElementById('stat-requests-30d').textContent = stats.requests_30d; const rem = document.getElementById('stat-remaining'); rem.textContent = stats.remaining; rem.className = 'value' + (stats.remaining <= 2 ? ' danger' : stats.remaining <= 5 ? ' warn' : ''); const badge = document.getElementById('plan-badge'); badge.textContent = stats.plan.charAt(0).toUpperCase() + stats.plan.slice(1); badge.className = 'plan-badge ' + stats.plan;
} function applyProfile(me) { document.getElementById('user-email').textContent = me.email || localStorage.getItem('email') || '—'; injectAdminLink(Boolean(me.is_admin));
} async function loadMe() { const res = await fetch(API + '/api/me', { headers }); if (res.status === 401) { logout(); return; } if (!res.ok) return; applyProfile(await res.json());
} async function loadStats() { const res = await fetch(API + '/api/stats', { headers }); if (res.status === 401) { logout(); return; } const s = await res.json(); applyStats(s);
} function renderChatList(chats) { chatList.innerHTML = ''; chats.forEach(c => { const div = document.createElement('div'); div.className = 'chat-item' + (c.id === currentChatId ? ' active' : ''); div.addEventListener('click', (e) => { if (e.target.closest('.chat-actions')) return; openChat(c.id); }); const title = document.createElement('span'); title.className = 'chat-title'; title.textContent = c.title; const actions = document.createElement('span'); actions.className = 'chat-actions'; const ren = document.createElement('button'); ren.type = 'button'; ren.className = 'chat-rename'; ren.textContent = '✎'; ren.title = 'Переименовать'; ren.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); renameChat(c.id, c.title); }); const del = document.createElement('button'); del.type = 'button'; del.className = 'chat-delete'; del.textContent = '✕'; del.title = 'Удалить чат'; del.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); deleteChat(c.id); }); actions.appendChild(ren); actions.appendChild(del); div.appendChild(title); div.appendChild(actions); chatList.appendChild(div); });
} async function loadChats() { const res = await fetch(API + '/api/chats', { headers }); const chats = await res.json(); renderChatList(chats);
} async function loadDashboardBootstrap() { const res = await fetch(API + '/api/bootstrap', { headers }); if (res.status === 401) { logout(); return null; } if (!res.ok) { throw new Error('bootstrap failed'); } const data = await res.json(); window.__GEOMIND_BOOTSTRAP__ = data; if (data.me) { applyProfile(data.me); } if (data.stats) { applyStats(data.stats); } if (Array.isArray(data.chats)) { renderChatList(data.chats); } return data;
} async function deleteChat(chatId) { const confirmed = await showConfirm('Удалить этот чат?'); if (!confirmed) return; await fetch(API + '/api/chats/' + chatId, { method: 'DELETE', headers }); if (currentChatId === chatId) { currentChatId = null; setLastAssistantText(''); messagesDiv.innerHTML = ` <div class="empty-state"> <div class="icon">🗺️</div> <div>Задайте вопрос по ГИС-алгоритмам</div> <div style="font-size:13px">Например: «Как построить буферную зону вокруг дороги?»</div> </div>`; } loadChats();
} function showConfirm(message) { return new Promise(resolve => { const overlay = document.createElement('div'); overlay.style.cssText = 'position:fixed;inset:0;background:rgba(0,0,0,0.5);display:flex;align-items:center;justify-content:center;z-index:9999;'; const box = document.createElement('div'); box.style.cssText = 'background:var(--bg-secondary);border:1px solid var(--border);border-radius:12px;padding:24px;max-width:340px;width:90%;text-align:center;box-shadow:0 8px 32px rgba(0,0,0,0.4);'; box.innerHTML = ` <div style="font-size:15px;color:var(--text-primary);margin-bottom:20px;">${message}</div> <div style="display:flex;gap:10px;justify-content:center;"> <button id="confirm-cancel" style="padding:8px 20px;border-radius:8px;border:1px solid var(--border);background:var(--bg-primary);color:var(--text-secondary);cursor:pointer;font-size:14px;">Отмена</button> <button id="confirm-ok" style="padding:8px 20px;border-radius:8px;border:none;background:var(--danger);color:#fff;cursor:pointer;font-size:14px;font-weight:600;">Удалить</button> </div>`; overlay.appendChild(box); document.body.appendChild(overlay); box.querySelector('#confirm-ok').focus(); box.querySelector('#confirm-ok').addEventListener('click', () => { overlay.remove(); resolve(true); }); box.querySelector('#confirm-cancel').addEventListener('click', () => { overlay.remove(); resolve(false); }); overlay.addEventListener('click', (e) => { if (e.target === overlay) { overlay.remove(); resolve(false); } }); });
} function renameChat(chatId, oldTitle) { const items = chatList.querySelectorAll('.chat-item'); for (const item of items) { const titleEl = item.querySelector('.chat-title'); if (titleEl && titleEl.textContent === oldTitle) { const input = document.createElement('input'); input.type = 'text'; input.value = oldTitle; input.className = 'chat-rename-input'; input.style.cssText = 'flex:1;background:var(--bg-primary);color:var(--text-primary);border:1px solid var(--accent);border-radius:4px;padding:2px 6px;font-size:13px;outline:none;min-width:0;'; titleEl.replaceWith(input); input.focus(); input.select(); const save = async () => { const newTitle = input.value.trim(); if (newTitle && newTitle !== oldTitle) { await fetch(API + '/api/chats/' + chatId, { method: 'PATCH', headers, body: JSON.stringify({ title: newTitle }), }); } loadChats(); }; input.addEventListener('keydown', (e) => { if (e.key === 'Enter') { e.preventDefault(); save(); } if (e.key === 'Escape') { loadChats(); } }); input.addEventListener('blur', save); break; } }
} async function openChat(chatId) { currentChatId = chatId; loadChats(); const res = await fetch(API + '/api/chats/' + chatId + '/messages', { headers }); const msgs = await res.json(); renderMessages(msgs);
} function getLastUserMessage(msgs) { for (let i = msgs.length - 1; i >= 0; i--) { if (msgs[i].role === 'user' && typeof msgs[i].text === 'string') return msgs[i].text; } return '';
} function renderMessages(msgs) { messagesDiv.innerHTML = ''; if (!msgs.length) { messagesDiv.innerHTML = emptyState.outerHTML; setLastAssistantText(''); return; } msgs.forEach(m => addMessageBubble(m.role, m.text)); setLastAssistantText(getLastAssistantMessage(msgs)); lastUserQuery = getLastUserMessage(msgs);
} function makeCopyBtn(getText, extraClass) { const btn = document.createElement('button'); btn.type = 'button'; btn.className = 'msg-action-btn' + (extraClass ? ' ' + extraClass : ''); btn.title = 'Копировать'; btn.innerHTML = ICON_COPY; btn.addEventListener('click', () => { navigator.clipboard.writeText(getText()).then(() => { btn.innerHTML = ICON_CHECK; btn.classList.add('copied'); setTimeout(() => { btn.innerHTML = ICON_COPY; btn.classList.remove('copied'); }, 1500); }); }); return btn;
} function addCodeCopyButtons(container) { container.querySelectorAll('pre').forEach(pre => { const wrap = document.createElement('div'); wrap.className = 'code-block-wrap'; pre.parentNode.insertBefore(wrap, pre); wrap.appendChild(pre); const btn = document.createElement('button'); btn.type = 'button'; btn.className = 'code-copy-btn'; btn.title = 'Копировать код'; btn.innerHTML = ICON_COPY; btn.addEventListener('click', () => { const code = pre.querySelector('code'); navigator.clipboard.writeText(code ? code.textContent : pre.textContent).then(() => { btn.innerHTML = ICON_CHECK; btn.classList.add('copied'); setTimeout(() => { btn.innerHTML = ICON_COPY; btn.classList.remove('copied'); }, 1500); }); }); wrap.appendChild(btn); });
} function addMessageBubble(role, text) { if (emptyState && messagesDiv.contains(emptyState)) { messagesDiv.innerHTML = ''; } const wrap = document.createElement('div'); wrap.className = 'message-wrap ' + role; const div = document.createElement('div'); div.className = 'message ' + role; if (role === 'assistant') { const inner = document.createElement('div'); inner.className = 'message-content'; inner.innerHTML = renderAssistantHtml(text); div.appendChild(inner); addCodeCopyButtons(inner); } else { div.textContent = text; } wrap.appendChild(div); const actions = document.createElement('div'); actions.className = 'msg-actions'; actions.appendChild(makeCopyBtn(() => text)); if (role === 'assistant') { const regenBtn = document.createElement('button'); regenBtn.type = 'button'; regenBtn.className = 'msg-action-btn'; regenBtn.title = 'Сгенерировать заново'; regenBtn.innerHTML = ICON_REGEN; regenBtn.addEventListener('click', regenerate); actions.appendChild(regenBtn); } wrap.appendChild(actions); messagesDiv.appendChild(wrap); messagesDiv.scrollTop = messagesDiv.scrollHeight; if (role === 'assistant') { setLastAssistantText(text); }
} function showTyping() { const div = document.createElement('div'); div.className = 'typing-indicator'; div.id = 'typing'; div.innerHTML = '<span></span><span></span><span></span>'; messagesDiv.appendChild(div); messagesDiv.scrollTop = messagesDiv.scrollHeight;
} function hideTyping() { const t = document.getElementById('typing'); if (t) t.remove();
} function toggleBugReportPanel() { if (!reportBugPanel) return; const shouldOpen = reportBugPanel.hidden; reportBugPanel.hidden = !shouldOpen; if (shouldOpen) { setBugReportStatus(''); if (reportBugText) reportBugText.focus(); }
} async function submitBugReport() { if (!reportBugText || !reportBugSendBtn) return; const text = reportBugText.value.trim(); if (!text) { setBugReportStatus('Введите описание ошибки', 'error'); reportBugText.focus(); return; } reportBugSendBtn.disabled = true; setBugReportStatus('Отправка...'); try { const res = await fetch(API + '/api/bug-reports', { method: 'POST', headers, body: JSON.stringify({ text, chat_id: currentChatId, page_url: window.location.href, }), }); const data = await res.json().catch(() => ({})); if (!res.ok) { setBugReportStatus(data.detail || 'Не удалось отправить', 'error'); return; } reportBugText.value = ''; setBugReportStatus('Отправлено', 'success'); window.setTimeout(() => { if (reportBugPanel) reportBugPanel.hidden = true; setBugReportStatus(''); }, 1500); } catch (e) { setBugReportStatus('Ошибка соединения', 'error'); } finally { reportBugSendBtn.disabled = false; }
} async function regenerate() { if (!lastUserQuery || !currentChatId) return; showTyping(); sendBtn.disabled = true; try { const res = await fetch(API + '/api/ask', { method: 'POST', headers, body: JSON.stringify({ query: lastUserQuery, chat_id: currentChatId }), }); hideTyping(); if (res.status === 403) { addMessageBubble('assistant', 'Лимит запросов исчерпан. Обновите тариф.'); return; } const data = await res.json(); addMessageBubble('assistant', data.answer); loadStats(); } catch (e) { hideTyping(); addMessageBubble('assistant', 'Ошибка соединения с сервером'); } finally { sendBtn.disabled = false; }
} async function send() { const query = queryInput.value.trim(); if (!query) return; lastUserQuery = query; queryInput.value = ''; addMessageBubble('user', query); showTyping(); sendBtn.disabled = true; try { const res = await fetch(API + '/api/ask', { method: 'POST', headers, body: JSON.stringify({ query, chat_id: currentChatId }), }); hideTyping(); if (res.status === 403) { addMessageBubble('assistant', 'Лимит запросов исчерпан. Обновите тариф.'); return; } const data = await res.json(); currentChatId = data.chat_id; addMessageBubble('assistant', data.answer); loadChats(); loadStats(); } catch (e) { hideTyping(); addMessageBubble('assistant', 'Ошибка соединения с сервером'); } finally { sendBtn.disabled = false; }
} sendBtn.addEventListener('click', send);
queryInput.addEventListener('keydown', (e) => { if (e.key === 'Enter') send();
}); newChatBtn.addEventListener('click', () => { currentChatId = null; setLastAssistantText(''); messagesDiv.innerHTML = ` <div class="empty-state"> <div class="icon">🗺️</div> <div>Задайте вопрос по ГИС-алгоритмам</div> <div style="font-size:13px">Например: «Как построить буферную зону вокруг дороги?»</div> </div>`; loadChats();
});
if (exportDocxBtn) exportDocxBtn.addEventListener('click', exportLastAnswerDocx);
if (exportPdfBtn) exportPdfBtn.addEventListener('click', exportLastAnswerPdf);
if (reportBugBtn) reportBugBtn.addEventListener('click', toggleBugReportPanel);
if (reportBugSendBtn) reportBugSendBtn.addEventListener('click', submitBugReport);
if (reportBugText) { reportBugText.addEventListener('keydown', (e) => { if ((e.ctrlKey || e.metaKey) && e.key === 'Enter') { e.preventDefault(); submitBugReport(); } });
} function logout() { localStorage.removeItem('token'); localStorage.removeItem('email'); window.location.href = '/static/login.html';
}
logoutBtn.addEventListener('click', logout); window.__geomindBootstrapPromise = loadDashboardBootstrap().catch(() => { document.getElementById('user-email').textContent = localStorage.getItem('email') || '—'; loadMe(); loadStats(); loadChats(); return null;
});
updateExportButtons();